.placeholder {
  display: grid;
  gap: var(--space-sm);
}

.lead {
  font-size: var(--font-size-lg);
  font-weight: 800;
  margin: 0;
}

.note {
  color: var(--color-text-muted);
  line-height: 1.5;
  margin: 0;
}
